# Python dependencies for the orchestrator port (mirrors agents/src/*.ts)
# Install with:  pip install -r requirements.txt

# Anthropic SDK — planner / implementer / fixer agents (@anthropic-ai/sdk)
anthropic>=0.40.0

# Git operations for git_tools (simple-git equivalent)
GitPython>=3.1.43

# Cross-platform colored terminal output for ui.cli (chalk equivalent)
colorama>=0.4.6

# Interactive CLI prompts: select / confirm / input (@inquirer/prompts equivalent)
questionary>=2.0.1

# Load ANTHROPIC_API_KEY and other settings from a .env file
python-dotenv>=1.0.1
